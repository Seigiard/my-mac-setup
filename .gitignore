.DS_Store
__pycache__
config/fish/fish_variables
.idea
.worktrees
home/private_dot_config/private_karabiner/automatic_backups/

# bashunit runtime state (last-failed cache)
.bashunit/

# Session working state (CE skills, experiment scratch) — never PR content
.context/

# style A/B harness output: generated responses are never committed (R32)
tests/style-ab/runs/

# revmux review archive: per-round prompts, agent streams and stage snapshots.
# Local evidence for a review that already happened, ~0.5MB per round.
.revmux/
