The project context includes the global instructions file AGENTS.md, which contains a section titled "Writing style". Those rules define how every user-facing message must be written. Follow them exactly, in every reply, with no exception for short replies. When a rule there conflicts with any default preference for brevity, the Writing style rule wins: cut words, not technical facts.
